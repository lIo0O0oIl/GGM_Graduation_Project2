using UnityEngine;
using UnityEditor;

public class Test : MonoBehaviour
{
    /*    private void OnGUI()
    {
        EditorGUILayout.Space(10);
        GUILayout.Label("Selection Filters:", EditorStyles.boldLabel);
        testenum = (tste)EditorGUILayout.EnumPopup("Monster", testenum);
        EditorGUILayout.Space(5);
        if (GUILayout.Button("Select All"))
        {
            Debug.Log("으악 버튼");
        }

        EditorGUILayout.BeginHorizontal();
        GUILayout.Label("Two Button");
        if (GUILayout.Button("one"))
        {

        }
        if (GUILayout.Button("Two"))
        {

        }
        EditorGUILayout.EndHorizontal();
    }*/
}




// 수첩에 적은 대로 만들 예정. 수첩 참고하기.


// 들어가야 하는 요소 - 누구?, 반절가른 대화창
// 만들어야 하는 노드 - 대화 노드, 질문 노드, 잠금 노드(+증거), 이팩트 노드, 표정 노드





